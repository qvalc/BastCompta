{
  "name": "bastcompta",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/validate-project.mjs && node --test tests/*.test.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
